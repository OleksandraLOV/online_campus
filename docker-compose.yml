services:
  server:
    build: ./server
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - JWT_SECRET=campus-secret-key-change-in-production

  client:
    build: ./client
    ports:
      - "5173:80"
    depends_on:
      - server
