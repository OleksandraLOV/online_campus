export enum SurveyStatus {
  DRAFT = 'draft',
  ACTIVE = 'active',
  CLOSED = 'closed',
}

export enum SurveyTargetType {
  ALL = 'all',
  GROUPS = 'groups',
  COURSE = 'course',
}

export enum SurveyQuestionType {
  SINGLE = 'single',
  MULTIPLE = 'multiple',
  RATING = 'rating',
  TEXT = 'text',
}
